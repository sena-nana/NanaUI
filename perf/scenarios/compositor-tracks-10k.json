{
  "schema_version": 1,
  "id": "compositor-tracks-10k",
  "kind": "Animation",
  "params": {
    "active": 10000,
    "class": "compositor",
    "properties": "mixed",
    "workload": "scale"
  },
  "record": ["work_counters", "steady_ms"],
  "invariants": [
    {
      "name": "compositor_scale_no_uiworld_mutations",
      "path": "work_counters.uiworld_mutations_from_animation",
      "op": "eq",
      "value": 0,
      "status": "required",
      "note": "10k compositor tracks must not write UiWorld on a present tick."
    },
    {
      "name": "compositor_scale_tracks",
      "path": "work_counters.motion_tracks_compositor",
      "op": "eq",
      "value": 10000,
      "status": "required",
      "note": "Echo params.active. CPU work must not be per-track UiWorld writes."
    },
    {
      "name": "compositor_scale_sparse_considered",
      "path": "work_counters.animations_considered",
      "op": "lte",
      "value": 8,
      "status": "required",
      "note": "CPU consider count must not grow linearly with compositor tracks."
    }
  ],
  "notes": "Issue #87 10k compositor-track scale. Work counters prove the fast path; timing is supplementary."
}
