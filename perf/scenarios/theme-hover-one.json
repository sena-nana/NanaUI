{
  "schema_version": 1,
  "id": "theme-hover-one",
  "kind": "Theme",
  "params": {
    "workload": "hover",
    "controls": 1000
  },
  "record": [
    "work_counters"
  ],
  "invariants": [
    {
      "name": "theme_hover_stays_local",
      "path": "work_counters.style_nodes_considered",
      "op": "lte",
      "value": 16,
      "status": "required",
      "note": "Issue #100 §15: hovering one control must not become a document-wide theme resolve. Deliberately a small constant, not a fraction of the fixture."
    },
    {
      "name": "theme_hover_does_not_reshape",
      "path": "work_counters.text_nodes_from_style",
      "op": "eq",
      "value": 0,
      "status": "required",
      "note": "A hover paint change is not a text change."
    },
    {
      "name": "theme_hover_does_not_lay_out",
      "path": "work_counters.layout_nodes_from_style",
      "op": "eq",
      "value": 0,
      "status": "required",
      "note": "A hover paint change is not a layout change."
    }
  ],
  "notes": "Issue #101 §4 single-node hover on a 1k-control fixture."
}
