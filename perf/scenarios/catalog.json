{
  "schema_version": 1,
  "issue": 8,
  "harness_ids": [
    "static-tree-100",
    "static-tree-1k",
    "static-tree-5k",
    "static-tree-10k",
    "static-tree-50k",
    "mutation-paint-only",
    "mutation-text",
    "mutation-layout-style",
    "mutation-visibility",
    "mutation-transform",
    "mutation-a11y",
    "hover",
    "virtual-list-10k",
    "virtual-list-100k",
    "virtual-tree-10k",
    "virtual-tree-100k",
    "text-table",
    "animation",
    "ime",
    "dock-workspace",
    "overlay",
    "text-editor",
    "gpu-scene-ui"
  ],
  "required_by_issue_not_in_harness": [
    {
      "id": "virtual-list-1m",
      "kind": "VirtualList",
      "params": {
        "items": 1000000,
        "visible": 40,
        "overscan": 8,
        "text_len": 32,
        "item_extent_px": 20
      },
      "note": "Required by #8 §3.4. Scenario JSON and Nana mapping exist; nana-framework-benchmark skips the 1M row unless NANA_PERF_SCALE=large. Not in harness_ids so default --all is not exit 2."
    },
    {
      "id": "virtual-tree-1m",
      "kind": "VirtualTree",
      "params": {
        "items": 1000000,
        "visible": 40,
        "overscan": 8,
        "text_len": 32,
        "item_extent_px": 20
      },
      "note": "Required by #8 §3.4. Scenario JSON and Nana mapping exist; nana-framework-benchmark skips the 1M tree row unless NANA_PERF_SCALE=large. Not in harness_ids so default --all is not exit 2."
    },
    {
      "id": "gpu-scene-ui-live2d",
      "kind": "GpuScene",
      "params": { "composition": "UiLive2d" },
      "note": "Required by #8 §3.8."
    },
    {
      "id": "gpu-scene-ui-live2d-effect",
      "kind": "GpuScene",
      "params": { "composition": "UiLive2dEffect" },
      "note": "Required by #8 §3.8. Required by #8 / not implemented as a shared Scenario runner."
    }
  ]
}
